{% extends 'base.html.twig' %}

{% block title %}Modification du mot de passe{% endblock %}

{% block content %}
    <h1>Modifier mon mot de passe</h1>
    
    <a href="{{path('app_account')}}">Retourner vers 'mon compte'</a> 

    {% if notification %}
        <div class="alert-info">{{ notification }}</div>        
    {% endif %}

    {{ form(form) }}
     
{% endblock %}